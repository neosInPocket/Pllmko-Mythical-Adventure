using UnityEngine;

public class ObjectsSpawnHandler : MonoBehaviour
{
	[SerializeField] private SpawnPool coinsPool;
	[SerializeField] private SpawnPool orbsPool;
	[SerializeField] private SpawnPool spikesPool;

	private void Start()
	{

	}
}
